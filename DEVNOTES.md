# 開發須知

往場景裡加東西之前要知道的規矩。

這裡不是 API 文件——每一支的用法寫在它自己的註解裡，而且寫得比這裡詳細。
這裡收的是另一種東西：**不寫下來就會忘、忘了也不會有人發現**的那幾條。
碰撞體標錯了畫面上一模一樣，只有跑起來覺得「這裡怪怪的」，而怪在哪沒有人
說得出來。

適用範圍是 `public/test-area/`（試玩場）。驗證是
`npm run verify:test-area`，加完東西一定要跑。

---

## 一、碰撞是分類的，不是布林的

視覺上的凹凸不准傳到腳底下。所以每一個碰撞體都必須說自己是哪一種，而
「哪一種」決定了它的盒子怎麼算、頂面可以在哪。

| 種類 | 是什麼 | 規矩 |
|---|---|---|
| `floor` | 可以站的水平面（鋪面、露台、牆頂、台座） | 盒子就是那個東西本身；頂面不准落在 0.08～0.36 那一段（會絆腳） |
| `block` | 繞得過、爬不上的障礙（柱、井、火盆、雕像、大石） | 盒子從它**站著的那個地面**（`base`）一路拉到頂，底下不准留縫；頂面至少 `BLOCK_TOP`（1.0） |
| `step` | 階梯的一級 | 只有階梯可以把頂面放在會絆腳的高度上，因為踩上去是預期的；級差不准超過 `PHYS.step` |
| `shell` | 不是給人站、但就是實心（牆體、台基） | 同 `floor` 的絆腳限制 |

沒有第五種。`solid: true` 留著當 `'block'` 的簡寫，但驗證會逐個檢查上面
那幾條，所以「隨手登記一個盒子」不是一個選項。

---

## 二、形狀：方的還是圓的

種類說的是「這個東西在遊戲裡是什麼」，形狀說的是「它長什麼樣」，兩件事
互不干涉：一根柱子是圓的 `block`，露台是方的 `floor`。

照著問下去：

**1. 畫面上是圓的嗎？** 不是 → 方盒（`block()`，或 `add` 的 `solid`）。到此為止。

**2. 是圓的 → 最大的 XZ 截面在哪？** 那個高度以下是圓柱，半徑就是那一圈。

> 方盒的角比它所代表的圓遠 41%。繞著一根方盒柱子走，身體會在四個角上各被
> 頂開一次——而畫面上那四個角不存在，所以那個頓挫找不到對應的東西。

**3. 最大截面以上是什麼形狀？**

| 上面是 | 怎麼登記 | 結果 |
|---|---|---|
| 平的（柱頂、井口、台座） | `round(x, z, r, y0, y1)` | 純圓柱。**站得住**，沒有第二條規則 |
| 鼓起來的（石頭、屋頂、圓塔頂） | 再給 `dome`（圓頂多高）與 `slip` | 圓頂。站不住，往下滑 |

幾何本身是一顆石頭那種「腰最寬」的形狀的話，`add` 可以自己算：
`solid: 'block', round: 'dome'`——腰以上就是圓頂。

半徑取**最大**的那一圈，寧可胖一點：比石頭瘦的碰撞體會讓身體陷進石頭裡，
而那個穿幫比「繞得遠了五公分」明顯得多。

---

## 三、圓頂上怎麼滑：`'slide'` 還是 `'fall'`

這是這份文件真正的理由。兩種滑法差的不是形狀，是**這個東西本來就給不給
站**——而那是砌的時候才知道的事，從幾何猜不出來，所以它是一個標記。

### 標 `'fall'`（失去操作的滑落）

**危險、不可踩的東西。**

這一類的碰撞存在的理由不是「擋住」——擋住是 `block` 在做的事。它是為了
**防止意外踩上非預期的位置**：跳躍落點失準、被別的東西推上去、從高處掉下來
剛好落在上面，這些都會發生，而那個位置不該站得住。人站上去的那一刻要立刻
知道「這裡不是給我站的」，所以回饋是拿走操作權、把人甩下來，而不是無聲地
讓他站著。

符合下面任何一條：

- **頂部是由多個難以設計的複雜形狀堆出來的。** 枝椏、一堆鐵器、一叢尖刺
  ——那不是一個踩得住的面，而要把它做成踩得住的面，得先設計出那個面。
- **設定上就危險或不可踩。** 陷阱、熔爐、聖物。形狀能不能站是另一回事。

行為：踩上去**操作整個失效**，沿著面加速 `g·sinθ`，直到被甩出去。控制權在
離開那個面的瞬間就回來（那時候人在空中，還救得回來）。

沒有體積的東西不要登記。火焰逐幀在抖，抖的東西當不了碰撞體，而一團火焰也
沒有一個表面可以讓人踩——所以火盆的碰撞到缽口為止，上面什麼都沒有。

### 標 `'slide'`（可操作的緩滑）

其他全部：屋頂、斜坡、圓塔頂、大石。

行為：站得住、操作得動、跳得掉，但**不跳就會一路緩滑下去**。速度是終端
速度 `SLIDE.max × sinθ`，不是加速度。這就是 2D 跑酷遊戲抓牆、蹬牆的那個
狀態：牆留得住你，重力不會停，而跳鍵隨時把你帶走。

### 想不出理由就標 `'slide'`

拿走玩家的操作權要說得出為什麼。標錯成 `'slide'` 的東西只是滑了一下，標錯成
`'fall'` 的東西會讓人覺得遊戲壞了。

### 幾個數字

| | `'slide'` | `'fall'` |
|---|---|---|
| 式子 | 位移速度 `SLIDE.max × sinθ` | 加速度 `g × sinθ` |
| 45° 時 | 1.41 m/s（走路是 4、衝刺是 8） | 15.6 m/s² |
| 操作 | 全部照常，跳得掉 | 全部失效，跳不掉 |
| 頂端 | 18° 以內站得住（`SLIDE.stick`） | 沒有站得住的一點（`SLIDE.kick`） |

`'slide'` 用終端速度而不是加速度，是因為積分下去的話一片大一點的屋頂會把人
加速到超過衝刺速——那是摔下去，不是抓牆。

---

## 四、不會有人提醒你的幾個數字

全部從 `PHYS` 算出來（`public/test-area/src/walk.js`），改了跳躍就全部跟著變，
所以**不要把它們抄成常數**，要用就 import。

| | 值 | 意思 |
|---|---|---|
| `PHYS.radius` / `height` | 0.30 / 0.92 | 身體是這麼粗的一根圓柱 |
| `PHYS.step` | 0.36 | 抬腳的高度。比這矮的東西走得上去，不必跳 |
| `TRIP` | 0.08～0.36 | 會絆腳的那一段。純視覺的碎石一律壓進地板、只露 0.08 以下 |
| `APEX` | 1.38 | 跳躍的頂點 |
| `MOUNT` | 1.74 | 跳得上去的最高頂面。**比這矮的障礙物都是平台** |
| `BLOCK_TOP` | 1.0 | 障礙物頂面的下限 |

---

## 五、區塊是用自己的局部座標砌的

每個區塊砌完會被 `shift()` 整段平移到世界位置上——頂點、墨線、碰撞體、火焰、
驗證用的那三份登記，全部。

**所以碰撞體上任何一個新的世界座標欄位都要記得在 `shift()` 裡搬。**
圓柱的軸（`x`／`z`）就是這樣漏過一次：外接盒搬了、軸沒搬，於是王座廳的柱子
擋在中庭的空地上。畫面上完全正常——那幾根柱子還在王座廳，只是它們的碰撞
跑到六十公尺外去了。

---

## 六、底下與外面：`open` 與 `air`

驗證會把每個場地走得到的地方全部淹一遍，而有兩件事它自己分不出是設計
還是 bug，所以要在碰撞體上標出來。

| 旗標 | 意思 | 用在哪 |
|---|---|---|
| `open` | 這個東西**底下**本來就是空的，走進去是對的 | 水窖的懸臂石階 |
| `air` | 空氣牆：擋身體、不擋鏡頭、不畫 | 城牆步道的女牆外框 |

**沒標 `open` 的東西，底下不准走得進去。** 台體、樓梯、平台一律從地面
砌起（碰撞也是）。城牆平台就是這樣壞過一次：台體只登記了最上面兩公尺，
底下那 1.2 公尺比狗還高，整座露台的肚子是一片走得進去的空地。

**人站在高處、外面是落差的場地，用空氣牆把人留在上面**（`B.air()`），
而不是把黑牆縮到走道邊上——黑牆是場地的外框，縮進來就看不到牆外。
空氣牆沿著女牆的外框從走道面立到封頂，名冊上那一筆加 `fenced: <走道面高度>`，
驗證就改驗「從那一層走、跳都下不去，只有感測區（門）上下得了」。

**每一個走得到的地方都要走得回出生點或一個出口。** 石階與牆之間的縫、門洞
被擋住之後的凹槽、牆與黑牆之間的縫，都是「掉得進去、爬不出來」。出口是送到
別的區塊的感測區——走得出去的地方不是困住。

**坑（`B.pit()`）一定要配一個感測區（`B.portal()`）。** 坑是故意掉得進去、
爬不出來的地方（窄巷那口井），它的出口就是感測區。感測區不是碰撞體，不進
`colliders`——那張清單上的每一支程式都在問「擋不擋」，混進去就得每一支都學會
跳過它。驗證會檢查每個坑底都在一個感測區裡，並且真的從坑口掉下去一次。

---

## 六之一、區塊之間的路：感測區、到達點、門

區塊互相看不到，所以區塊之間的路是感測區：`B.portal()`（圓）或
`B.portalBox()`（方），走進去就被送到 `to` 指的地方——`'alley'` 是那個區塊的
出生點，`'alley.fog'` 是那個區塊用 `B.arrive()` 登記的到達點，`'.towerTop'`
是自己這個區塊的到達點。名字寫錯，`buildRuins()` 會直接丟錯。

- **到達點擺在對面那個感測區外面。** 擺在裡面，一落地就又被送回來，兩邊來回彈。
  到達點可以在半空中（井掉進水窖就是從三公尺高落下）。
- **不是單向的路要有回程**：目的地那一邊有一個感測區送回這一個旁邊。單向的
  （井）在 `portal()` 給 `{ oneWay: true }`。
- **門是一組一組的**：同一組的幾扇門一起開關（`{ door: 'tower' }`），名冊上
  `doors: { tower: false }` 是一開始的狀態。門關著，屬於它的感測區就不存在。
  兩種狀態的門扇用 `B.detach()` 各砌一塊，不進合併的那一份，執行時只換看得到
  的那一塊（main.js 的 `setDoor`，試玩時按 O）。
- **門開著，門洞走得進去**：圓柱碰撞可以開屬於一組門的缺口（`B.round(…, { notch })`），
  那一組門開著、身體的中心在缺口裡，那根圓柱就不擋；洞裡擋人的是另外登記的盒子
  （門框、頂板、甬道的盡頭）。所以 `solveXZ` 跟 `portalAt` 一樣要拿門的狀態，
  沒給就是全部關著。圓塔門洞的感測區在門面內 0.5：狗整隻走進門裡才被送走。
- **門洞裡只能有門框。** 開著的門洞看得進去，而牆是一皮一皮錯開半塊砌的：門洞
  旁邊那一段牆的端磚會多出半塊斜插進洞裡，一路砌過來的牆（幕牆砌到塔心）會
  橫在甬道中間。前者給 `wall()` 的 `flush`（那段高度的磚裁到端頭），後者讓那一面
  牆停在門框石外側。開著那一塊登記了 `hole`（門洞的盒子），驗證會量它。

驗證（「傳送點」「門」那兩項，以及整片淹水時門的每一種狀態各淹一遍）會檢查：
到達點站得住、不在任何感測區裡、回程存在而且從到達點真的走得進去；每一組門兩種
狀態都有幾何、開著時有進有出、門洞裡沒有門框以外的三角形。

---

## 七、材料是照顏色認的

石紋、木紋、灰泥、瓦、鐵、布（`surface.js`）不在零件裡指定：`Build.add`
拿那一塊的**顏色**去查 `palette.js` 的 `SURF_OF`，查到哪一種就上哪一種的紋路。
所以零件照舊只給 `color`，而：

- **新加一個顏色，表裡沒有它就是素色。** 不會報錯，畫面上就是一塊沒有紋路的
  東西。新顏色是新材料的話，記得加進 `SURF_OF`；只有個別零件要例外，在
  `add` 給 `surf`。
- **紋路順著一塊幾何最長的那一軸走**（木紋順著木料、瓦沿著簷口）。所以一根
  木料要用長條的幾何去砌，不要用一塊方的再壓扁——壓扁的縮放也算，但三軸差不多
  長的時候取 x，那個方向不一定是你要的。
- **不要從零件的亂數器抽「只影響外觀」的東西。** 缺角的變體、每一塊的深淺、
  紋路的偏移，全部用 `hashAt(位置)` 決定。多抽一個亂數，後面哪根柱子斷、哪裡
  撒碎石就全部換掉了——版面會悄悄變，驗證抓不到「跟以前不一樣」。

**苔量是每一張圖自己的**：名冊（`BLOCKS`）的 `moss`，0～1，不給就是 1。
它同時管石頭頂面的苔與苔叢，所以新零件要撒苔叢一律呼叫 `mossTuft`（它自己
看苔量），不要自己 `add` 一塊綠色的東西——那一塊不會跟著苔量走。

`/test-area/?surf=0` 關掉紋路（著色器不接、貼圖不算），其他一模一樣。同一台
手機開關各看一次 fps，差的就是紋路的成本。

---

## 八、加完就跑驗證

```bash
npm run verify:test-area
```

它會踩過每一片空地、從每個出生點走到中心、把走得到的地方淹一遍、掃過每一塊石頭底下有沒有東西
頂著、繞著每一根圓柱走一圈、在每一個圓頂上滑一次。這些全部是**看不出來有
沒有壞**的東西——這就是它存在的理由。

紅了就是真的壞了，不要調門檻。
